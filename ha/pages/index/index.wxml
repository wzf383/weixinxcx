<view class="page">
<view class="top"><text >底部可刷新</text></view>
  <view class="body">
    <view class="list" wx:for="{{array}}"   >
      <view class="item" id="{{item.id}}" bindtap="short">
       ★  {{item.title}}
      </view>
    </view>
  </view>

 
  <view>
  
  <scroll-view scroll-y style="height: 200px;"  bindscrolltolower="onReachBottom" bindscroll="scroll" scroll-into-view="{{toView}}" scroll-top="{{scrollTop}}">

  </scroll-view>
  </view>
</view>